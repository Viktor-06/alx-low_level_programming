#include "main.h"

/**
 * _abs - computes te absolute value of an integer
 * @n: the int to check
 * Return: the absolute valuse of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
